#include <stdio.h>

/**
* main - Print different combinations of 3 digits
* Return: always zero
*/

int main(void)
{
    for (int i = 48; i < 58; i++)
    {
        if (i !== j && j !== k)
        putchar(i);
        putchar(j);
        putchar(k);
        putchar(44);
        puthchar(32)
    }
    putchar()
}